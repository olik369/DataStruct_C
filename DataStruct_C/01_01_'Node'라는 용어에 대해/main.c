#include <stdio.h>

int main(int argc, char* argv[])
{
	return 0;
}

/*
* - Node라는 용어에 대해
* 
* 자료구조를 적용해 관리해야 할 대상 자료에 대한 기본 단위
* 구조체나 객체로 기술하는 것이 일반적
* 
* 노드는 개념적용어로 메모리 덩어리라고 생각하면 됨
* 
* - Single linked list
* 
* 연결 리스트는 여러 구조체 인스턴스를 체인처럼 포인터로 연결한 자료구조
* 연결에 사용된 포인터 숫자가 한 개이고 자기 다음을 가리키는 것이 특징
*/